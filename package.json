{
  "name": "openai-agent-sdk-test",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Agent Guardian (WatchMyAgents WGS) — analyse d'exports d'actions, scoring de risque déterministe et proposition de politiques, sur l'OpenAI Agents SDK (TypeScript)",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "selfcheck": "tsx src/selfcheck.ts",
    "watch": "tsx src/watch.ts",
    "smoke": "tsx src/smoke-test.ts",
    "capture-fixtures": "tsx src/capture-fixtures.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "dependencies": {
    "@openai/agents": "^0.2.0",
    "dotenv": "^16.4.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
